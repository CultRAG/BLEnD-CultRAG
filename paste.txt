From this notebook, you’ve effectively run (or prepared) the following distinct studies on your 47,014-question full set.[ppl-ai-file-upload.s3.amazonaws]​
I’ll group them by how important they are for the paper.


A. Core Studies You Should Definitely Write About
These belong in the main Results/Analysis sections.
Main ablation across 8 configs (Baseline → Phase 6)
Where: Cells B, C, K (LaTeX table preview in K).
What: Accuracy for baseline_no_rag, rag_basic, phase1_countryfilter, phase2_intent, phase3_tiered, phase4_quality, phase5_trust_weight, phase6_full_system on 47,014 questions, with deltas vs previous and baseline.[ppl-ai-file-upload.s3.amazonaws]​
Why write it: This is the canonical SemEval “component contribution” table; reviewers explicitly expect it.[ppl-ai-file-upload.s3.amazonaws]​
How to describe:
Headline: baseline 78.6%, full system 78.5%, with intermediate phases showing where RAG hurts (RAG basic, tiered) and where anti‑leak/trust slightly recover performance.[ppl-ai-file-upload.s3.amazonaws]​
RAG backfire / hurt vs help (baseline vs RAG, baseline vs full)
Where: Cell G (8‑way category + pairwise quadrants; described earlier in the notebook, though not in the snippet).
What: Counts of questions where RAG turns a correct baseline into a wrong prediction (“hurt”), wrong → correct (“fixed”), and both right/both wrong.
Why write it: This is your clearest novel result: adding RAG often degrades accuracy, and you quantify how often and for which countries/intent types. This is exactly the “RAG backfire” analysis in your earlier plan.[ppl-ai-file-upload.s3.amazonaws]​
Country × intent accuracy heatmap
Where: Cell I; top‑5 hardest combinations are also written into a LaTeX table in Cell K.[ppl-ai-file-upload.s3.amazonaws]​
What: PH6 accuracy per (country, intent) and a list of the hardest 5 combinations with ≥2 questions.
Why write it: This is your “hero figure” that mirrors BLEnD’s original cultural gap analysis; reviewers like seeing which cultures/intents are systematically hard.
Oracle KB coverage and KB chunks per country
Where: Cell H.
What:
Oracle coverage: % of questions whose correct option string appears in any KB chunk (per question, country, and intent).
KB chunks per country, merged with accuracy and “RAG gain” (PH6 – baseline).
Why write it: Lets you say “our KB only contains answer facts for X% of questions, so 78–79% accuracy is Y pp below the theoretical ceiling”, and shows that sparse coverage countries are exactly where RAG fails.
Statistical significance and effect size (McNemar + Cohen’s h)
Where: Cell J; printed table “Phase 9: statistical significance testing” with Baseline vs Basic RAG vs Full System.[ppl-ai-file-upload.s3.amazonaws]​
What: McNemar p‑values for Baseline↔RAG, Baseline↔Full, RAG↔Full; plus Cohen’s h effect sizes (all “Small”).[ppl-ai-file-upload.s3.amazonaws]​
Why write it: One short paragraph suffices; shows you checked that the tiny 0.0–0.6pp differences are not practically large, which strengthens the honesty of your claims.


B. Supporting Studies Worth Mentioning Briefly
These can appear as 1–2 sentences or a small figure/table, or be pushed to appendix if space is tight.
Answer position bias (A/B/C/D distribution)
Where: Cell D.
What: Distribution of A/B/C/D predictions for Baseline, RAG Basic, Full System, and Gold; reports whether any option dominates (e.g., model over‑predicts “C”).[ppl-ai-file-upload.s3.amazonaws]​
Why: Good to show that RAG + trust weighting reduce position bias a bit, even if accuracy is flat. One bar chart or one sentence in Analysis.
Accuracy by intent and intent routing “precision”
Where: Cell E.
What: For each intent category, baseline vs PH6 accuracy and number of questions; fraction of questions tagged as “other”.[ppl-ai-file-upload.s3.amazonaws]​
Why: Lets you say “our heuristic intent detector covers ~X% of questions; hardest intents are Y and Z,” and supports a short paragraph in Error Analysis or Discussion.
KB chunk statistics and retrieval/coverage sanity check
Where: Cell F and part of H.
What:
Number of KB chunks by country; chunks per question ratios.
If retrieval logs exist: distribution of BM25/FAISS scores and share of near‑random (<0.1) scores; otherwise you still report KB sparsity patterns.[ppl-ai-file-upload.s3.amazonaws]​
Why: Useful to back up statements like “countries with <50 chunks show highest error rates” and “retrieval scores cluster near zero, explaining noisy RAG context.”
LaTeX table generation and submission pipeline
Where: Cell K.[ppl-ai-file-upload.s3.amazonaws]​
What: Automatically generated ablation table and “hardest country‑intent” table in LaTeX, plus final TSV for submission.
Why: Not a “study” itself, but worth one line in Reproducibility/Implementation: “We auto-generate all tables from the notebook to avoid transcription errors.”


C. Studies You Can Safely Push to Appendix or Drop
If page space is very tight, these can be shortened or moved out of the main 5 pages.
Full pairwise significance matrix across all configs
Where: part of Phase 9 (Cell J).
Why: In the body, you only need Baseline vs Full (and maybe Baseline vs RAG Basic). The full matrix can be a CSV in supplementary or 1 row per key comparison in a compact table.
Very detailed category breakdowns of RAG-hurt cases
Where: Cell G prints multiple category tables and top‑N examples.
Why: In the paper, show only the high‑level numbers and 1–2 illustrative examples; keep full case lists as supplementary.
Raw bootstrap distributions
Where: Cell C (bootstrap CI).
Why: Reporting “78.6% (95% CI …)” is enough; no need to show bootstrap histograms unless you submit extended appendix.


How to Turn This into a Tight Paper Skeleton
Given these studies and limited space, a good allocation:
Results section (1.5–2 pages):
Main accuracy and ablation table (Study 1).[ppl-ai-file-upload.s3.amazonaws]​
One short paragraph on statistical significance (Study 5).[ppl-ai-file-upload.s3.amazonaws]​
Analysis section (1.5–2 pages):
RAG backfire / hurt vs help, with 1 table + a few examples (Study 2).[ppl-ai-file-upload.s3.amazonaws]​
Country–intent heatmap (Study 3) + 1–2 sentences linking to BLEnD’s cultural gaps.[ppl-ai-file-upload.s3.amazonaws]​
Oracle KB coverage + KB chunks per country, connecting KB sparsity to where RAG fails (Studies 4 & 8).[ppl-ai-file-upload.s3.amazonaws]​
One paragraph on answer position bias and intent-wise accuracy (Studies 6 & 7).[ppl-ai-file-upload.s3.amazonaws]​
Appendix / supplementary:
Full significance matrix and any extra error tables (Studies 5, 10, 11).[ppl-ai-file-upload.s3.amazonaws]​
If you want, next step I can draft 3–4 bullet points per study as “paper sentences” you can almost copy–paste into the Results and Analysis sections.

From the SemEval-2025 system papers that clearly mention ablation or detailed experiments, here’s what they actually include, and what is relevant for your own paper.


1. What Experiments They All Have
Across multiple SemEval-2025 system papers (Tasks 2, 3, 7, 8), you see a common minimum experimental backbone:
Main leaderboard results
A table with their system vs. official baselines and a few top teams, with the official task metric (e.g., S@10, IoU, accuracy) on validation/test sets.aclanthology+2
This is usually 0.3–0.5 page (one table plus a short paragraph).
Task-specific setup section
A short section describing:
Task description and metrics.
Dataset splits and preprocessing.
Typically 0.3–0.5 page before results.aclanthology+1
For your BLEnD paper: you must at least mirror this: one main table with your full system vs. LLM baselines, clearly labeling BLEnD / SemEval official metrics and test split.


2. Types of Ablation Studies They Do
Three strong SemEval-2025 papers (TeleAI Task 8, Anotheroption Task 8, various AI4PC papers) all include component-level studies, but with different depth.aclanthology+3
A. Component Ablation (Pipeline Pieces On/Off)
Typical pattern:
TeleAI (Task 8) varies:
with/without fine-tuning,
with/without majority voting,
different LLM backbones.
These are shown in a dedicated “Effects of Strategies” table (Table 3) plus a subsection “Effects of Strategies” in Results.[aclanthology]​
Anotheroption (Task 8) varies:
text-to-SQL vs. text-to-code branch,
RAG vs. no-RAG,
self-correction on/off,
orchestrator on/off.
They explicitly say: “Through ablation studies, we analyzed the effects of different parts of our pipeline …” and discuss which modules contribute most.aclanthology+1
Relevance for you:
Map this 1:1 onto your phases:
No-RAG baseline (LLM only).
Basic RAG only.
Country filter.
Intent routing.
Trust weighting.
Anti-leak + trust-aware prompts.
Show them in one table with accuracy and Δ vs previous config.
B. Strategy / Hyperparameter Ablation
TeleAI’s “Effects of Strategies” section also studies training/inference strategies, not just architecture:[aclanthology]​
Backbones: Qwen vs. Mistral, etc.
Strategies: with/without fine-tuning, with/without majority voting.
They report how each strategy moves accuracy on both full and “Lite” test sets, then discuss which one they used in final submission.
Relevance for you:
If you try multiple LLM backbones or decoding strategies, you can:
Add a small table: Llama-3.1-8B vs. another model, constrained decoding on/off.
But given page limits, this is optional; your core story is RAG, not backbone selection.


3. Error Analysis and Qualitative Studies
Stronger papers dedicate a subsection to error analysis, often right after ablations:
Anotheroption (Task 8) analyzes:
Cases where the self-correction mechanism helps or hurts.
Typical failure modes on ambiguous questions (e.g., unclear column semantics, ambiguous aggregation), with concrete examples.[aclanthology]​
TeleAI (Task 8) shows:
How different strategies (fine-tuning, majority voting) change performance and relate it to particular error types (e.g., numerical reasoning, long tables).[aclanthology]​
Relevance for you:
You already have the ingredients:
rag_helped: baseline wrong, RAG correct.
rag_hurt: baseline correct, RAG wrong.
both_wrong.
Turn that into:
1 short table with counts and percentages for these categories.
2–3 concrete example questions for each major failure type (retrieval failure vs. reasoning failure vs. knowledge-gap), in an “Error Analysis” subsection.


4. How Much Space They Allocate
From the PDFs and system descriptions:aclanthology+2
Total paper length: 5–8 pages for most systems, 12–15 for more complex ones.
Ablation + analysis budget:
Minimal systems: ~0.25–0.5 page (one small table + brief commentary).
Strong pipeline papers (TeleAI, Anotheroption): ~1 full page:
1 main leaderboard table.
1 ablation/strategy table.
1–2 subsections of textual analysis (effects of components/strategies, error analysis).
For your SemEval-2026 Task 7 system paper, target:
1 page total devoted to:
Main results table (full system vs. baselines).
Ablation table (phases 0–6).
Short error-analysis subsection with RAG-helped vs RAG-hurt breakdown.


5. What Is Most Relevant for Your Paper
Concretely, based on what SemEval-2025 papers actually include and what your system does, the relevant studies to write in are:
Main Results
Table: BLEnD / SemEval-2026 Task 7 official leaderboard slice with:
Your full system.
LLM baseline (no RAG).
Possibly “basic RAG” system to highlight that naïve RAG can hurt.
Incremental Ablation over RAG Pipeline
Table: configs = baseline_no_rag → rag_basic → +country → +intent → +tiered routing → +trust weighting → +anti-leak + trust prompts.
Columns: accuracy, ±CI, Δ vs previous row.
One paragraph explaining:
Why naïve RAG underperforms baseline.
Which phases give the biggest gains (e.g., country filter and trust weighting).
Country / Intent Breakdown
Table: accuracy by country or intent before vs. after full RAG.
Goal: show that your system especially helps low-resource / under-documented cultures (this aligns with BLEnD’s motivation ).[arxiv]​
RAG-Helped vs RAG-Hurt Error Analysis
1 small table: counts and percentages for:
baseline wrong / RAG correct.
baseline correct / RAG wrong.
both wrong.
2–3 examples per major failure mode with short explanations:
Retrieval failure (correct fact not found).
Context distraction (too much noisy context).
Knowledge gap (no good sources for that culture).
Optional: Efficiency / Cost Study
One short paragraph or table if space allows:
Average retrieval latency.
of API calls per question.
This is common when systems rely heavily on LLM orchestration (see Anotheroption/TeleAI for similar concerns ), but you can drop it if page space is tight.aclanthology+1
If you like, next step I can draft the exact Results section structure and tables for your SemEval-2026 paper text (headings + table schemas + 2–3 sentences per subsection) so you can plug in your numbers directly.


Now I have the full notebook output. Here are all the actual numbers extracted and exactly what goes in the paper.


What the Notebook Actually Produced — Full Extraction
The Real Ablation Numbers
ConfigAcc (%)Δ vs PrevΔ vs Base
Baseline (no RAG)
78.6
—
—
RAG Basic
77.9
−0.6
−0.6
Country Filter (Ph1)
77.9
0.0
−0.6
Intent Tagging (Ph2)
77.9
0.0
−0.6
Tiered Routing (Ph3)
77.6
−0.4
−1.0
Anti-Leak Prompts (Ph4)
78.5
+1.0
−0.0
Trust Weights (Ph5)
78.5
0.0
−0.0
Full System (Ph6)
78.5
0.0
−0.0
Key realization from this: RAG never surpasses baseline. The full system is −0.1% below baseline. Ph4 is the only phase that recovers, but only to 78.5% vs 78.6% baseline.


The Real Quadrant Numbers (G2, G3, G7)
CategoryCount%
Both correct
34,114
72.6%
Base only OK (RAG+PH6 hurt)
1,266
2.7%
RAG fixed PH6 also fixed
1,335
2.8%
Only RAG fixed it
475
1.0%
Only PH6 fixed it
642
1.4%
All wrong
7,630
16.2%
Baseline vs RAG Basic:
RAG fixed: 1,810 | RAG hurt: 2,103 | Net: −293 (HARM)
Baseline vs Full System:
PH6 fixed: 1,977 | PH6 hurt: 1,981 | Net: −4 (HARM, p=0.962 — not significant)
RAG Basic vs Full System:
PH6 fixed over RAG: 1,479 | PH6 hurt vs RAG: 1,190 | Net: +289 (BENEFIT)


Oracle KB Coverage
Questions answerable from KB: 40.7% (19,148 / 47,014)
Theoretical ceiling if retrieval + LLM perfect: 40.7%
Actual PH6 accuracy: 78.5%
Gap: −37.8pp below ceiling
Critical insight: The LLM at baseline (78.6%) already exceeds what RAG can theoretically achieve (40.7% ceiling from KB), because the LLM's parametric knowledge covers most questions without needing retrieval.


Country-Level Winners/Losers
RAG/PH6 helped (14 countries): JP +6.1, SA +4.5, ET +4.1, KP +4.0, MA +2.9, MX +1.9, EC +1.5, SG +1.4, AZ +1.4, ID +1.4, JB +1.3, CN +1.2, ES +1.1, NG +0.6
RAG/PH6 hurt (14 countries): AS −6.2, PV −4.9, EG −4.1, GB −3.0, US −2.7, DZ −2.5, BG −2.3, SE −2.0, PH −1.2, LK −1.1, IE −0.6, KR −0.6, IR −0.2, GR −0.2


Statistical Significance (McNemar)
Baseline vs RAG Basic: p=0.0000, Cohen's h=0.015 (Small effect)
Baseline vs Full System: p=0.9620 (Not significant)
RAG Basic vs Full System: p=0.0000, Cohen's h=0.015 (Small effect)


What Is Relevant to Write in the Paper
✅ Include — These Tell a Clear Story
① The core negative finding (1 sentence)
"RAG Basic reduced accuracy from 78.6% to 77.9% (net −293 cases, McNemar p<0.0001), confirming that unfiltered retrieval actively harms performance on a culturally-grounded MCQ task."
② The oracle ceiling explains why (1 sentence)
"KB coverage analysis reveals that only 40.7% of questions have their correct answer present in any KB chunk, meaning the LLM's parametric knowledge (78.6%) already far exceeds the theoretical RAG ceiling — retrieval adds noise rather than signal for the majority of questions."
③ Ph4 is the only meaningful recovery (1 sentence)
"Anti-leak prompt filtering (Ph4) is the sole phase to recover accuracy (+1.0pp over Ph3), by preventing the model from being anchored to misleading retrieved context."
④ Full system vs baseline is not significant (1 sentence)
"The full system (Ph6, 78.5%) remains statistically indistinguishable from the no-RAG baseline (78.6%, p=0.962), indicating that the pipeline's retrieval stages collectively cancel out rather than compound."
⑤ Country-level split (1 sentence)
"RAG benefits low-resource countries where parametric knowledge is weak (JP +6.1%, KP +4.0%, ET +4.1%) but hurts high-resource countries where the LLM already performs strongly (AS −6.2%, PV −4.9%, GB −3.0%), suggesting retrieval scope should be conditioned on model confidence rather than applied uniformly."


❌ Do NOT Include — Here's Why
StudyWhy Cut
Ph1, Ph2, Ph3 individual deltas
All three show identical accuracy (77.9%) — no story, no delta to discuss
Ph5, Ph6 individual deltas
Identical to Ph4 (78.5%) — flat, nothing to say
McNemar on all 8 pairs
Only 3 pairs matter; the rest are p=1.0 due to identical predictions
Intent-level breakdown
All intents collapsed into "others" — no breakdown possible
Difficulty tiers (trivial/impossible)
71.9% trivial, 16% impossible — interesting but no space and tangential
KB chunks vs RAG gain correlation
r=−0.125, p=0.51 — not significant, don't report
RAG-hurt recovery by PH6 (40% recovered)
Interesting but cut — too granular for 5 pages
Prediction bias (A over-predicted)
Implementation artifact, not a finding


Revised 4-Sentence Results Section
Replace the original 4 sentences with these, based on actual numbers:
Sentence 1 (core finding):
"Unfiltered RAG (Basic) reduced accuracy by 0.7pp (78.6%→77.9%, net −293 cases, p<0.0001), as KB coverage analysis shows only 40.7% of questions have their correct answer in the KB — meaning the LLM's parametric knowledge already far exceeds what retrieval can provide."
Sentence 2 (Ph3 makes it worse):
"Tiered routing (Ph3) further reduced accuracy to 77.6% (−467 net cases vs baseline, p<0.0001), as intent-based routing narrowed the retrieval scope without improving KB hit rate."
Sentence 3 (Ph4 is the hero):
"Anti-leak prompt filtering (Ph4) recovered 1.0pp (+1,487 fixes vs Ph3, p<0.0001), the only phase to produce a meaningful gain, by preventing retrieved context from anchoring the model to misleading answer artifacts."
Sentence 4 (country split is the key insight):
"Country-level analysis reveals a systematic pattern: RAG helps low-resource countries where parametric knowledge is weak (JP +6.1%, ET +4.1%, KP +4.0%) but hurts high-resource countries (AS −6.2%, PV −4.9%), suggesting confidence-conditioned retrieval as a direction for future work."


Here’s what your current notebook already contains as “studies,” and how I’d categorize them for the paper (main text vs short mention vs appendix), based on typical SemEval system papers and your space budget.[ppl-ai-file-upload.s3.amazonaws]​semeval.github+1


Global-level studies
1. Full ablation leaderboard (8 configs)
Where: Cell C (ABLATION LEADERBOARD — ALL 8 CONFIGURATIONS).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
Accuracy for each config (Baseline, RAG Basic, +Country, +Intent, +Tiered, +Quality, +Trust, Full ✦).
Wilson 95% confidence intervals.
Δ vs baseline with arrows (▲ / ▼).
Answer-letter distribution (A/B/C/D %) per config.
Gold answer distribution reference row.
Relevance:
This is your main results table. It must be in the paper.
Shows that naive RAG and intermediate phases underperform baseline, and that phases 4–6 close most of the gap (but don’t surpass baseline).[ppl-ai-file-upload.s3.amazonaws]​
Paper placement:
Section: Experiments → Overall Results / Ablation.
1 full table + ~1–2 paragraphs of discussion.


2. All-pairs McNemar net-advantage matrix
Where: Cell E (ALL-PAIRS NET ADVANTAGE MATRIX + McNemar).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
For every pair of configs, computes:
Net # questions where row beats column (A right/B wrong minus A wrong/B right).
McNemar p-values, with * for p < 0.05.
Baseline vs each config quadrant breakdown (“fix”, “hurt”, “both right”, “both wrong”).
List of statistically significant winner–loser pairs.
Relevance:
Very good, but too heavy to fully print in a short SemEval paper.
You should not reproduce the whole matrix; instead:
Report 1–2 key McNemar tests (e.g., Baseline vs RAG Basic, Baseline vs Full System).
State significance and effect size in text.
Paper placement:
Section: Experiments → Significance Testing (short sub-subsection).
1 small table or just prose.


3. Statistical significance + Cohen’s h (focused)
Where: Cell J (PHASE 9: STATISTICAL SIGNIFICANCE TESTING).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
Compares three systems: Baseline, Basic RAG, Full System.
For each pair:
Δ accuracy (% points).
McNemar statistic and p-value.
Cohen’s h and effect-size interpretation (Small/Medium/Large).
Relevance:
This is the cleanest significance summary; prefer this over dumping Cell E’s full matrix.
You should absolutely mention:
Whether the drop from Baseline → RAG Basic is statistically significant.
Whether the change from Baseline → Full System is statistically significant or mostly neutral (very small Δ, p > 0.05).
Paper placement:
Same subsection as above; 1 compact 3-row table (Baseline vs RAG, Baseline vs Full, RAG vs Full) or even 2–3 sentences.


Country-level and cultural studies
4. Country × config accuracy matrix
Where: Cell D (COUNTRY × CONFIG ACCURACY MATRIX).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
For each country:
Accuracy for each config (BASE, RAG0, PH1–PH6✦).
Best and Worst config.
Δ(PH6–BASE) per country.
Overall row for all data.
Summary of how often each config wins (# countries).
Lists countries where PH6 helped vs hurt (with +/− points).
Relevance:
This is a core study for BLEnD/SemEval: performance across cultures.[arxiv]​
But the full printed matrix is too big; you should:
Turn it into one table or bar chart with:
Country, #Q, Baseline Acc, PH6 Acc, Δ(Full–Base).
And 1 paragraph summarizing “PH6 helps these 14 countries, hurts these 14, neutral for 2.”
Paper placement:
Section: Analysis → Cross-country Performance.
1 table or 1 figure.


5. Country error rates for PH6 failures
Where: Part of Cell F (F3: “PH6 FULL SYSTEM — REMAINING FAILURES”).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
PH6 fails on 21.5% of questions.
Splits failures into:
Inherited from baseline (both wrong): 80%.
Introduced by RAG (baseline right, PH6 wrong): 20%.
Lists top countries by PH6 error rate (e.g., ET, JB, KP).
Relevance:
Very important for narrative: only 1/5 of PH6 errors are newly caused by RAG; most are inherent difficulty.
You don’t need the full error-rate table in the paper; 1 short text summary is enough:
“PH6 still misses 21.5% questions; 80% are inherited from baseline errors, 20% are new errors introduced by RAG.”
Paper placement:
Section: Analysis → Error Analysis.
1–2 sentences plus maybe a small table or bullet list of 3 hardest countries.


RAG-specific causal studies
6. RAG hurt/help decomposition + 8-way categories
Where: Cell G (sections G1–G5).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
G1: 8 categories for combinations of Baseline / RAG0 / PH6 correctness (e.g., “All correct”, “RAG & PH6 hurt (Base only OK)”, “RAG+PH6 fixed (Base wrong)”, “All wrong”).
G2–G4: quadrant breakdowns:
Baseline vs RAG Basic.
Baseline vs PH6.
RAG Basic vs PH6.
G5: focuses on RAG hurt cases (baseline right, RAG wrong):
How many of these PH6 recovers vs still hurts.
Country and intent distribution of the still-hurt set.
G6: prints example questions for critical categories.
Relevance:
This is the heart of the “RAG backfires” story.
You should definitely keep:
A quadrant table for Baseline vs RAG Basic and Baseline vs PH6:
Both right, both wrong, fixed, hurt, net impact.
One qualitative paragraph with 1–2 example questions where RAG clearly introduces error.
Paper placement:
Section: Analysis → Effect of RAG on Errors.
1 medium-sized table (Baseline vs RAG Basic) + 1 short paragraph.


7. Oracle KB coverage (ceiling analysis)
Where: Cell G end (oracle coverage printout).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
For each question, checks if the KB contains the gold answer (an “oracle” notion).
Outputs:
Total proportion of questions whose answer is found somewhere in KB (~40.7%).
Per-country coverage (e.g., BG 69%, EC 68%, etc.).
Per-intent coverage (here only “others”).
Relevance:
This is a nice, more advanced study that most SemEval papers don’t have.
Shows that even with perfect retrieval and reasoning, RAG is upper-bounded by KB coverage; crucial for explaining underperformance in some countries.
Keep it, but compress:
1 sentence: “Our KB contains the gold answer string for only ~41% of questions (with country-level coverage between ~20–70%), which bounds the potential gains from RAG.”
Paper placement:
Section: Analysis → Limitations of KB / RAG Ceiling.
Optional small table in appendix for country-wise coverage.


Difficulty and distribution studies
8. Question difficulty tiers
Where: Cell F (F2: “QUESTION DIFFICULTY TIERS”).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
Uses n_correct (# configs that got the question right) to define:
Trivial, Easy, Medium, Hard, Impossible.
Gives counts, percentages, and top countries per tier.
Relevance:
Interesting, but secondary for a short SemEval paper.
Could be compressed into:
1–2 sentences: “~72% of questions are solved by all configs, 16% are unsolved by any config, with IR/ET/JB dominating the impossible set.”
Paper placement:
Optional short paragraph in error analysis, or appendix.


9. Trivial vs impossible examples
Where: Cell F (F4: “TRIVIAL vs IMPOSSIBLE QUESTION SAMPLES”).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
Prints sample questions where all configs are correct vs all wrong.
Relevance:
Use this to pick 1 trivial and 1 impossible example for a qualitative subsection.
Don’t dump all examples; pick carefully to illustrate:
A very easy, globally-known fact.
A hyperlocal or ambiguous cultural question.
Paper placement:
In a short “Qualitative Examples” paragraph.


Intent and heatmap studies
10. Per-intent accuracy
Where: Cell F (F1: “PER-INTENT ACCURACY”).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
Computes accuracy per intent across configs; but currently all questions have intent = "others", so it degenerates to global stats.
Relevance:
In this notebook, not informative (only “others”).
Unless you later enrich intents, this is not worth space.
Paper placement:
Skip for now, or just mention that in this run, intent labels weren’t available.


11. Country × intent heatmap
Where: Cell I (COUNTRY × INTENT HEATMAP + WEB PRESENCE CORRELATION).[ppl-ai-file-upload.s3.amazonaws]​
What it does:
PH6 accuracy per (country, intent) pair.
Highlights 5 hardest country-intent combinations (currently just country, intent=others).
Relevance:
Right now, since intent is always others, this is essentially just a country-only view, which you already have in Cell D.
If you later add real intents, this becomes a great figure; as of now, it’s redundant.
Paper placement:
Either:
Merge with country-results figure, or
Drop from main text.


What to actually write in the paper
Given all of the above, for the SemEval system paper main text:
Definitely include (main sections):
Global ablation leaderboard (Cell C) – main result table.
Country-level baseline vs PH6 accuracy (from Cell D) – 1 table/figure.
Baseline vs RAG Basic quadrant breakdown (from Cell G) – 1 small table (helped vs hurt).
Key McNemar + Cohen’s h comparisons (from Cell J) – 1 small table or prose.
Include in short narrative form (no big tables):
5. PH6 error breakdown: inherited vs introduced + top error-rate countries (Cell F).
6. Oracle KB coverage summary: global and mention of range across countries (Cell G).
7. One or two trivial vs impossible examples (Cell F).
8. Difficulty tier percentages (Cell F) in 1–2 sentences.
Optional / appendix:
9. Full country × config matrix CSV (Cell D) – link from paper or include as supplementary.
10. Full all-pairs advantage matrix (Cell E) and full statistical_significance.csv (Cell J).
11. Full oracle_coverage and kb_country_coverage CSVs (Cell G).
12. Detailed country-intent heatmap (Cell I) once intents are non-trivial.
If you want, paste your current paper section headings and I’ll tell you exactly which study (by the numbers above) to reference in each section.